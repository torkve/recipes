# Runtime data: sqlite db, uploaded images, sessions. Backed up by archiving data/.
/data/

# Build artifacts
/recipes
/bin/
*.test
*.out

# Local environment (secrets); .env.example is committed
.env

# Editor / OS
.DS_Store
*.swp

# Original task spec; kept locally but not tracked (purged from history)
/plan.md

# Captured HAR files may contain real auth cookies/tokens — never commit
*.har
